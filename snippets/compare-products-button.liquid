{%- comment -%}
	------------------------------------------------------------------------------
	Snippet: Compare products button
	- product: Product (required, product)
	- additional_classes: Additional classes (optional, string)

	{%- render 'compare-products-button', product: product, additional_classes: "" -%}
	------------------------------------------------------------------------------
{%- endcomment -%}

<button
	class="product-card__quick-button {{ additional_classes }} is-hidden js-compare-products-button"
	data-product-handle="{{- product.handle -}}"
	data-product-title="{{- product.title | escape -}}"
	aria-label="{{ 'products.compare_products.product_button_add' | t: product: product.title | escape }}"
	type="button"
>
	<span class="product-card__quick-button-outer"></span>
	{%- render 'icons', icon: 'compare' -%}
</button>
